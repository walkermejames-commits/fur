import React from 'react'; import { View, Text } from 'react-native'; import { Button } from '../components/Button'; import { colors } from '../theme/colors';
export const FriendsScreen = ({ route, navigation }: any) => <View style={{flex:1,justifyContent:'center',alignItems:'center',gap:14,backgroundColor:colors.backgroundPrimary,padding:24}}><Text style={{fontSize:48,color:colors.gold,fontWeight:'900'}}>D</Text><Text style={{fontSize:28,fontWeight:'800',color:colors.cream}}>Connection ready</Text><Text style={{color:colors.textMuted,textAlign:'center'}}>You both want to connect.</Text><Button title='Say hi' onPress={()=>navigation.replace('Chat',{chatId:route.params.chatId})}/></View>;
